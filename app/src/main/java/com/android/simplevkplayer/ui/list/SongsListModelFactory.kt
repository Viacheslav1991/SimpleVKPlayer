package com.android.simplevkplayer.ui.list

import androidx.lifecycle.ViewModel
import androidx.lifecycle.ViewModelProvider
import com.android.simplevkplayer.data.player.SongsRepositoryImpl

/*
class SongsListModelFactory : ViewModelProvider.Factory {
    @Suppress("UNCHECKED_CAST")
    override fun <T : ViewModel?> create(modelClass: Class<T>): T  = when (modelClass){
        SongsListViewModel::class.java -> SongsListViewModel(SongsRepositoryImpl())
        else -> throw IllegalArgumentException("$modelClass is not registered ViewModel")
    } as T


}*/
